version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: postgres-db
    environment:
      POSTGRES_DB: questions
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Aranya
    ports:
      - "5432:5432"
    volumes:
      - ./postgresSQL:/var/lib/postgresql/data